DATABASE_URL="file:./dev.db"

# 32자 이상의 무작위 문자열. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
SESSION_SECRET=""

# 이메일 인증코드 실발송용 (미설정 시 화면에 코드가 표시되는 데모 모드로 동작)
# GMAIL_APP_PASSWORD는 일반 로그인 비밀번호가 아니라 Google 계정의 "앱 비밀번호"입니다.
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# developers.kakao.com에서 발급한 JavaScript 키.
# 앱의 "제품 설정 > 카카오맵"을 활성화하고, "플랫폼 > Web"에 사용할 도메인을 등록해야 합니다.
NEXT_PUBLIC_KAKAO_MAP_KEY=""
